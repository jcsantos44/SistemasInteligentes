#pragma once
#include "Estado.h"

bool operator == (const Estado &e1, const Estado &e2);

bool operator < (const Estado &e1, const Estado &e2);

bool operator != (const Estado &e1, const Estado &e2);
